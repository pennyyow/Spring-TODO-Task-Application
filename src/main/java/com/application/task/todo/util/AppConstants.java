package com.application.task.todo.util;

/**
 * Provides the class to handle hard-coded strings
 * and make it re-usable across the application
 */
public class AppConstants {
  /**
   * Constant as a default created by field value
   */
  public static final String ANONYMOUS = "Anonymous";

  /**
   * Constant as a default date format
   */
  public static final String DATE_FORMAT = "yyyy-MM-dd";
}
